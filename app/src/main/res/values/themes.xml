<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Gardening" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/terracotta</item>
        <item name="android:navigationBarColor">@color/terracotta</item>
    </style>

    <style name="Theme.Gardening" parent="Base.Theme.Gardening" />

</resources>